<UserControl x:Class="UI.Components.StudentList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.Components"
             xmlns:local2="clr-namespace:DataLayer.Model;assembly=DataLayer"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid Name="students" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" IsReadOnly="True" Binding="{Binding ID}"/>
                <DataGridTextColumn Header="Names" IsReadOnly="True" Binding="{Binding Names}"/>
                <DataGridTextColumn Header="Email" IsReadOnly="True" Binding="{Binding Email}"/>
                <DataGridTextColumn Header="Password" IsReadOnly="True" Binding="{Binding Password}"/>
                <DataGridTextColumn Header="Role" IsReadOnly="True" Binding="{Binding Role}"/>
                <DataGridTextColumn Header="Fac Number" IsReadOnly="True" Binding="{Binding FacNum}"/>
                <DataGridTextColumn Header="Expires" IsReadOnly="True" Binding="{Binding Expires}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
